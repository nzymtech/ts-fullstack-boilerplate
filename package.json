{
  "name": "ts-fullstack-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:web": "pnpm --stream -r run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit": "lint-staged",
    "prepare": "husky init"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2"
  }
}
